name: CI Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: cd backend && npm install

      - name: Lint
        run: cd backend && npm run lint

      - name: Tests
        run: cd backend && npm test

      - name: Build Docker image
        run: docker build -f docker/Dockerfile -t book-tracker:latest .

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push to DockerHub
        run: |
          docker tag book-tracker:latest ${{ secrets.DOCKERHUB_USERNAME }}/book-tracker:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/book-tracker:latest

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/book-tracker:latest
          format: table
          exit-code: '0'
